有一个地址段 192.168.10.0/24
以下地址，我们通过show arp 或者ping发现被使用,一个ip_list
ip_lits = ['192.168.10.2','192.168.10.16']
对192.168.10.0/24 进行子网划分，每次输入一个新的掩码，比如/26的时候，列出所有子网，以及是未被使用
未被使用的判断依据是ip_list中的地址均不落在此网段。


def subnet(ip_net,ip_list,new_prefix):
   ...
返回的格式如下
[{'ip_net':'192.168.10.0/26','used':True}] used代表被使用了。